# LASERTOOL v6.19

黑線稿模式單線化修正：

- 黑線稿預覽：Sobel 邊緣先做骨架細線化，不再把同一條線描成左右兩層。
- 線條粗細：在單一中心線完成後再加粗，因此視覺上仍是一條線。
- 黑線稿模式的 `DXF 檔案`：改為直接輸出中心線路徑，不再輸出黑線兩側的雙輪廓。
- `DXF 外框`：維持 v6.18，只輸出作品最外圈一條封閉線。
- 網點模式的 `DXF 檔案`：維持原功能。
